## Add props, validation & rendering patterns
### DELIVERABLE
Extend your Addis Eats React menu: a Dish component with PropTypes and a default, a conditionally rendered spicy badge, a category filter with an empty state, and the list rendered with stable keys — committed to GitHub.
### Steps
- [x]  Add PropTypes to Dish (name/price required, spicy optional) and a currency default.
- [x]  Render a "Spicy" badge conditionally with && (guard non- [x] booleans).
- [x]  Build a Card wrapper component that renders its children.
- [x]  Filter the menu by a category and show an empty state when none match.
- [x]  Render the filtered list with map, using each dish’s id as the key.
- [x]  Commit and push, checking the console for any PropTypes warnings